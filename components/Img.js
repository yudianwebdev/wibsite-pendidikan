// import { ImgHTMLAttributes } from "react";

// const Img = ({ src, ...props }: ImgHTMLAttributes<HTMLImageElement>) => {
//   src = src?.includes("http") ? src : `${process.env.APP_PREFIX}${src}`;

//   return <img src={src} {...props} />;
// };

// export default Img;
